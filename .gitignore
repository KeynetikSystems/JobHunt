.env
.venv/
__pycache__/
*.pyc
.qtcreator/
build/
dist/
*.spec
# Ledger.spec is hand-maintained (lists which QML files ship with the Windows/local
# PyInstaller build) — not a throwaway pyi-makespec artifact, so it's tracked.
!Ledger.spec
reports/
seen_items.json
queries.json
history.json
cv.txt
*.db
.claude/
