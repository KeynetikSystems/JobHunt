org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
org.gradle.daemon=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Kotlin's incremental-cache relativization crashes ("this and base files have
# different roots") when the pub cache (C:) and this project (E:) are on
# different drives — a real Windows cross-drive bug, not flakiness. Disabling
# incremental compilation avoids the code path entirely; full-rebuild only,
# slightly slower, but stable.
kotlin.incremental=false
